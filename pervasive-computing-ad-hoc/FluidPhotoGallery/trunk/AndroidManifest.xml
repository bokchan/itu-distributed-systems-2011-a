<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="dk.itu.spct"
      android:versionCode="1"
      android:versionName="1.0">
    <uses-sdk android:minSdkVersion="8" />

    <application android:icon="@drawable/icon" android:label="@string/app_name">
        
        
        <activity android:name=".FluidPhotoGalleryActivity"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 
        NB! important to set android:launchMode="singleInstance"
        because = 
        2. Switch activities, single instances
		The default behavior of the activities, when invoked by an Intent, 
		can be modified. In particular, it is possible to tell the system 
		to create just a single instance of an activity, and reuse that 
		instance if possible. Open the AndroidManifest.xml of the previous exercise.
		Set the Launch Mode of both the activities to singleInstance. 
		Run the project and see that, no matter how many times you switch 
		between the activities, it will require just 2 back-button 
		click to come back to the home activity.
        -->
        <activity android:name=".GalleryActivity" 
		          android:launchMode="singleInstance"></activity>

		<activity android:name=".TCPSendActivity" 
		          android:launchMode="singleInstance"></activity>

		<activity android:name=".TCPServerActivity" 
		          android:launchMode="singleInstance"></activity>
		          
		<activity android:name=".server.ServerConnect" 
		          android:launchMode="singleInstance"></activity>

        <activity android:name=".ImageActivity" 
		          android:launchMode="singleInstance"></activity>    

        <activity android:name=".tryouts.ActivitySecond" 
		          android:launchMode="singleInstance"></activity>    


		<activity android:name=".tryouts.ActivityFirst" 
		          android:launchMode="singleInstance"></activity>    




    </application>
    

    
    
    
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    
</manifest>